
#import "@preview/bytefield:0.0.8": *

#bytefield(
        bpr: 16,
        bits(1)[Junk],
        bits(1)[Ribbon],
        bits(1)[Animal],
        bits(1)[Bright],
        bits(1)[Double Junk],
        bits(4)[Suit],
        bits(6)[Index],
)
